{
  "name": "@resume-parser/web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev ../..",
    "build": "next build ../..",
    "start": "next start ../..",
    "typecheck": "tsc --noEmit -p ../../tsconfig.json",
    "test": "vitest run --config ../../vitest.config.ts"
  }
}
